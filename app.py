# from src.utils.helper import filter_uuid
# from src.logs import fetch_logs

# logs = fetch_logs(hours=1, limit=10000, page=1, page_size=50)

# uuid_map = filter_uuid(logs['logs'])

# print(uuid_map)